# Dependências diretas do projeto. Fonte de verdade é o pyproject.toml;
# este arquivo existe para quem quiser `pip install -r requirements.txt`
# sem o editable install. Regenerar a lista travada (com transitivas) só
# quando for empacotar: `pip freeze > requirements.lock`.
flet==0.86.5
